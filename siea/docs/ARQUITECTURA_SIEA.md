# ARQUITECTURA SIEA - Sistema Integral de Inteligencia Energética

## 📐 DIAGRAMA DE ARQUITECTURA GENERAL

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          CAPA DE PRESENTACIÓN                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌─────────────────┐  ┌──────────────────┐  ┌────────────────────┐     │
│  │  Dashboard Web  │  │  WhatsApp Bot    │  │  API REST Pública  │     │
│  │   (React/Dash)  │  │  (Business Cloud)│  │   (Terceros)       │     │
│  └────────┬────────┘  └────────┬─────────┘  └──────────┬─────────┘     │
│           │                     │                        │               │
│           └─────────────────────┴────────────────────────┘               │
│                                  │                                       │
└──────────────────────────────────┼───────────────────────────────────────┘
                                   │
                        ┌──────────▼──────────┐
                        │   NGINX / Ingress   │
                        │   (TLS 1.3)         │
                        └──────────┬──────────┘
                                   │
┌──────────────────────────────────┼───────────────────────────────────────┐
│                          CAPA DE APLICACIÓN                               │
├──────────────────────────────────┼───────────────────────────────────────┤
│                                  │                                        │
│  ┌───────────────────────────────▼────────────────────────────┐          │
│  │                      FastAPI Backend                        │          │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │          │
│  │  │ Health   │ │ Reports  │ │ Predict  │ │ Simulations  │  │          │
│  │  │ Router   │ │ Router   │ │ Router   │ │ Router       │  │          │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │          │
│  │                                                             │          │
│  │  ┌──────────────────────────────────────────────────────┐  │          │
│  │  │         Authentication & Authorization              │  │          │
│  │  │         (OAuth2 + Azure AD + MFA)                   │  │          │
│  │  └──────────────────────────────────────────────────────┘  │          │
│  └──────────────────────┬───────────────────┬──────────────────┘          │
│                         │                   │                             │
│         ┌───────────────┴──────┐    ┌───────▼──────────┐                 │
│         │                      │    │                  │                 │
│  ┌──────▼──────────┐   ┌───────▼────▼───┐   ┌────────▼───────────┐      │
│  │  Agente LLM     │   │  ML Inference  │   │   Simuladores      │      │
│  │  + RAG          │   │  Engine        │   │   (Hidrología,     │      │
│  │  (LangChain)    │   │  (Prophet,     │   │    Mercado,        │      │
│  │                 │   │   XGBoost)     │   │    Confiabilidad)  │      │
│  └────────┬────────┘   └────────┬───────┘   └────────────────────┘      │
│           │                     │                                        │
└───────────┼─────────────────────┼────────────────────────────────────────┘
            │                     │
┌───────────┼─────────────────────┼────────────────────────────────────────┐
│           │   CAPA DE DATOS     │                                        │
├───────────┼─────────────────────┼────────────────────────────────────────┤
│           │                     │                                        │
│  ┌────────▼────────┐   ┌────────▼──────────┐   ┌──────────────────┐    │
│  │   Vector DB     │   │   PostgreSQL      │   │    Redis         │    │
│  │   (Weaviate)    │   │   + TimescaleDB   │   │   (Cache +       │    │
│  │                 │   │                   │   │    Memoria)      │    │
│  │  - Documentos   │   │  - Demanda        │   │                  │    │
│  │  - Leyes        │   │  - Generación     │   │  - Sesiones      │    │
│  │  - Informes XM  │   │  - Pérdidas       │   │  - Rate Limit    │    │
│  │  - Embeddings   │   │  - Precios        │   │  - Queues        │    │
│  └─────────────────┘   └───────────────────┘   └──────────────────┘    │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    Data Lake (S3/GCS)                             │   │
│  │   Raw Layer → Bronze Layer → Silver Layer → Gold Layer           │   │
│  │   (Parquet files organizados por fecha y fuente)                 │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
└──────────────────────────────┬───────────────────────────────────────────┘
                               │
┌──────────────────────────────┼───────────────────────────────────────────┐
│                   CAPA DE INGESTA (ETL)                                  │
├──────────────────────────────┼───────────────────────────────────────────┤
│                              │                                           │
│  ┌───────────────────────────▼─────────────────────────────────┐        │
│  │              Orquestador (Airflow / Prefect)                 │        │
│  │                                                               │        │
│  │  DAG Diario:    ETL → Validación → Incremental Training     │        │
│  │  DAG Semanal:   Full Retraining → Backtesting               │        │
│  │  DAG Mensual:   Reports → Model Evaluation                  │        │
│  └───────────────────────────┬─────────────────────────────────┘        │
│                              │                                           │
│         ┌────────────────────┼────────────────────┐                     │
│         │                    │                    │                     │
│  ┌──────▼──────┐   ┌─────────▼────────┐   ┌──────▼─────────┐           │
│  │  Extractors │   │  Transformers    │   │   Loaders      │           │
│  │             │   │                  │   │                │           │
│  │  - XM API   │   │  - Normalize     │   │  - PostgreSQL  │           │
│  │  - SUI API  │   │  - Validate      │   │  - Data Lake   │           │
│  │  - DANE     │   │  - Clean         │   │  - Vector DB   │           │
│  │  - CREG     │   │  - Aggregate     │   │                │           │
│  │  - News     │   │  - Anonymize     │   │                │           │
│  └─────────────┘   └──────────────────┘   └────────────────┘           │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────┐
│                    FUENTES DE DATOS EXTERNAS                              │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│   ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐           │
│   │  XM  │  │ SUI  │  │ CREG │  │ UPME │  │ DANE │  │MinMi │           │
│   └──────┘  └──────┘  └──────┘  └──────┘  └──────┘  └──────┘           │
│                                                                           │
│   ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐              │
│   │  OpenAI API  │  │ WhatsApp API │  │  datos.gov.co    │              │
│   │  (GPT-4)     │  │  (Meta)      │  │  (Datos abiertos)│              │
│   └──────────────┘  └──────────────┘  └──────────────────┘              │
│                                                                           │
└──────────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────────┐
│                    INFRAESTRUCTURA (Kubernetes)                           │
├──────────────────────────────────────────────────────────────────────────┤
│                                                                           │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐             │
│  │   Prometheus   │  │    Grafana     │  │   ELK Stack    │             │
│  │   (Métricas)   │  │  (Dashboards)  │  │    (Logs)      │             │
│  └────────────────┘  └────────────────┘  └────────────────┘             │
│                                                                           │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐             │
│  │  Alertmanager  │  │   MLflow       │  │   SonarQube    │             │
│  │  (Alertas)     │  │  (ML Tracking) │  │  (Code Quality)│             │
│  └────────────────┘  └────────────────┘  └────────────────┘             │
│                                                                           │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 FLUJO DE DATOS DETALLADO

### 1. Flujo ETL (Extracción, Transformación, Carga)

```
┌─────────────┐
│   Fuente    │  XM: Demanda horaria, Precios bolsa, Generación
│   (XM API)  │  Endpoint: https://www.xm.com.co/consumo-demanda
└──────┬──────┘
       │ HTTPS + API Key
       ▼
┌──────────────┐
│  Extractor   │  - Autenticación
│  XM          │  - Rate limiting (max 100 req/min)
│              │  - Retry logic (3 intentos, backoff exponencial)
└──────┬───────┘
       │ JSON/CSV
       ▼
┌──────────────┐
│ Transformer  │  1. Validar schema (Pydantic)
│ Normalize    │  2. Limpiar datos (nulls, outliers)
│              │  3. Convertir unidades (MW → GWh)
│              │  4. Timestamp UTC
└──────┬───────┘
       │ Parquet
       ▼
┌──────────────┐
│  Data Lake   │  s3://siea-data-lake/raw/xm/demanda/2025/12/02/
│  (Raw Layer) │  Archivo: demanda_20251202.parquet
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  Loader      │  1. Insertar en PostgreSQL (tabla demanda_nacional)
│  PostgreSQL  │  2. Actualizar índices
│              │  3. Triggear notificación si hay alertas
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  Validador   │  - Verificar calidad de datos (DQ score)
│  Calidad     │  - Alertar si calidad < 90%
│              │  - Registrar métricas en Prometheus
└──────────────┘
```

---

### 2. Flujo de Predicción (ML Inference)

```
┌─────────────┐
│  Usuario    │  Solicita forecast de demanda para próximos 7 días
│  Dashboard  │  POST /api/v1/predict/demanda
└──────┬──────┘
       │ HTTPS + JWT Token
       ▼
┌──────────────┐
│  FastAPI     │  1. Validar autenticación (OAuth2)
│  Backend     │  2. Validar parámetros (Pydantic)
│              │  3. Verificar rate limit (Redis)
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  ML Engine   │  1. Cargar modelo desde MLflow
│  Inference   │  2. Obtener features (últimos 30 días)
│              │  3. Ejecutar predicción
│              │  4. Calcular intervalos de confianza
└──────┬───────┘
       │ Forecast + Confidence intervals
       ▼
┌──────────────┐
│  Auditoría   │  Registrar:
│  Logger      │  - Usuario, timestamp
│              │  - Modelo usado (version)
│              │  - Parámetros de entrada
│              │  - Resultado generado
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  Response    │  JSON con:
│  to User     │  - forecast: [valores por día]
│              │  - confidence: {lower: [], upper: []}
│              │  - model_version: "prophet_v2.3"
└──────────────┘
```

---

### 3. Flujo de Asistente Conversacional (RAG)

```
┌─────────────┐
│  Usuario    │  Escribe: "¿Cuáles son las pérdidas actuales?"
│  WhatsApp   │  Envía mensaje a número oficial MME
└──────┬──────┘
       │ HTTPS (WhatsApp Cloud API)
       ▼
┌──────────────┐
│  Webhook     │  1. Validar HMAC signature (Meta)
│  Endpoint    │  2. Extraer mensaje y remitente
│              │  3. Verificar whitelist de usuarios
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  Agent Core  │  1. Cargar memoria conversacional (Redis)
│  (LangChain) │  2. Clasificar intención (router)
│              │  3. Determinar herramientas necesarias
└──────┬───────┘
       │
       ├────────────────┐
       │                │
       ▼                ▼
┌──────────────┐  ┌──────────────┐
│  RAG Tool    │  │  SQL Tool    │
│              │  │              │
│ 1. Embedding │  │ 1. Generate  │
│ 2. Search    │  │    query     │
│    Vector DB │  │ 2. Execute   │
│ 3. Retrieve  │  │ 3. Format    │
│    Top-3     │  │    result    │
└──────┬───────┘  └──────┬───────┘
       │                │
       └────────┬───────┘
                ▼
┌──────────────────────┐
│  LLM (GPT-4)         │  Prompt:
│                      │  "Contexto: [documentos RAG + datos SQL]
│                      │   Pregunta: [pregunta usuario]
│                      │   Responde citando 3 fuentes."
└──────┬───────────────┘
       │ Respuesta generada
       ▼
┌──────────────────────┐
│  Post-procesamiento  │  1. Validar fuentes citadas
│                      │  2. Agregar disclaimers si aplica
│                      │  3. Formatear para WhatsApp
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  Envío respuesta     │  WhatsApp Cloud API
│  + Auditoría         │  + Registro completo en DB
└──────────────────────┘
```

---

## 🛡️ ARQUITECTURA DE SEGURIDAD

```
┌─────────────────────────────────────────────────────────────────────┐
│                         PERÍMETRO EXTERNO                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌────────────────┐         ┌────────────────┐                      │
│  │   WAF          │         │   DDoS         │                      │
│  │   (CloudFlare) │ ──────▶ │   Protection   │                      │
│  └────────┬───────┘         └────────────────┘                      │
│           │                                                          │
│           ▼                                                          │
│  ┌────────────────┐                                                 │
│  │   Firewall     │  Whitelist IPs:                                 │
│  │   (iptables)   │  - Ministerio: 192.168.x.x/24                   │
│  │                │  - VPN: 10.0.x.x/16                             │
│  └────────┬───────┘  - WhatsApp: 157.240.0.0/16                    │
│           │                                                          │
└───────────┼──────────────────────────────────────────────────────────┘
            │
┌───────────┼──────────────────────────────────────────────────────────┐
│           │         DMZ (Demilitarized Zone)                         │
├───────────┼──────────────────────────────────────────────────────────┤
│           │                                                          │
│           ▼                                                          │
│  ┌────────────────┐                                                 │
│  │ Ingress NGINX  │  - TLS 1.3 obligatorio                          │
│  │ + Cert Manager │  - Certificados Let's Encrypt (auto-renew)     │
│  │                │  - HTTP → HTTPS redirect                        │
│  └────────┬───────┘  - Rate limiting: 100 req/min por IP           │
│           │                                                          │
└───────────┼──────────────────────────────────────────────────────────┘
            │
┌───────────┼──────────────────────────────────────────────────────────┐
│           │         ZONA DE APLICACIONES                             │
├───────────┼──────────────────────────────────────────────────────────┤
│           │                                                          │
│           ▼                                                          │
│  ┌────────────────┐                                                 │
│  │ Authentication │  1. OAuth2 + JWT                                │
│  │ Gateway        │  2. Azure AD (SSO)                              │
│  │                │  3. MFA obligatorio (TOTP)                      │
│  │                │  4. Session timeout: 30 min                     │
│  └────────┬───────┘  5. IP whitelisting adicional                  │
│           │                                                          │
│           ▼                                                          │
│  ┌────────────────┐                                                 │
│  │ Authorization  │  IAM Policies:                                  │
│  │ (RBAC)         │  - admin: full access                           │
│  │                │  - analista: read + predict                     │
│  │                │  - viewer: read only                            │
│  │                │  Least-privilege por defecto                    │
│  └────────┬───────┘                                                 │
│           │                                                          │
│           ▼                                                          │
│  ┌────────────────────────────────────────┐                         │
│  │     Backend Services (Pods K8s)        │                         │
│  │  - NetworkPolicy: deny by default      │                         │
│  │  - PodSecurityPolicy: non-root         │                         │
│  │  - Resource limits (CPU, RAM)          │                         │
│  └────────┬───────────────────────────────┘                         │
│           │                                                          │
└───────────┼──────────────────────────────────────────────────────────┘
            │
┌───────────┼──────────────────────────────────────────────────────────┐
│           │         CAPA DE DATOS (Zona restringida)                │
├───────────┼──────────────────────────────────────────────────────────┤
│           │                                                          │
│           ▼                                                          │
│  ┌────────────────┐                                                 │
│  │ Secrets Mgmt   │  - KMS (AWS) / Key Vault (Azure)                │
│  │ (Sealed Secrets│  - Secrets nunca en código                      │
│  │  + KMS)        │  - Rotación automática cada 90 días            │
│  └────────┬───────┘  - Audit log de accesos                        │
│           │                                                          │
│           ▼                                                          │
│  ┌────────────────┐                                                 │
│  │ Databases      │  - TLS para conexiones                          │
│  │                │  - Cifrado at-rest (AES-256)                    │
│  │ PostgreSQL     │  - Backups cifrados                             │
│  │ Redis          │  - Access via service account only              │
│  │ Weaviate       │  - No acceso directo desde internet             │
│  └────────────────┘                                                 │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│                    MONITOREO Y AUDITORÍA                              │
├──────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌────────────────────────────────────────────────────────────┐     │
│  │  Audit Logging (SIEM)                                       │     │
│  │  - Todos los accesos (quién, cuándo, qué, desde dónde)    │     │
│  │  - Retención: 7 años                                        │     │
│  │  - Alertas automáticas:                                     │     │
│  │    * Login fallido > 3 intentos                             │     │
│  │    * Acceso fuera de horario laboral                        │     │
│  │    * Query con > 100K registros                             │     │
│  │    * Modificación de configuración crítica                  │     │
│  └────────────────────────────────────────────────────────────┘     │
│                                                                       │
│  ┌────────────────────────────────────────────────────────────┐     │
│  │  Security Monitoring (Prometheus + Grafana)                 │     │
│  │  - CPU/RAM por pod                                          │     │
│  │  - Requests/sec                                             │     │
│  │  - Error rate (threshold: < 0.1%)                          │     │
│  │  - Certificate expiration (alertar 30 días antes)          │     │
│  └────────────────────────────────────────────────────────────┘     │
│                                                                       │
└───────────────────────────────────────────────────────────────────────┘
```

---

## 📊 STACK TECNOLÓGICO COMPLETO

### Backend
| Componente | Tecnología | Versión | Justificación |
|------------|------------|---------|---------------|
| Framework | FastAPI | 0.109+ | Async, rápido, OpenAPI auto |
| ASGI Server | Uvicorn | 0.27+ | High performance |
| Validación | Pydantic | 2.5+ | Type safety |
| ORM | SQLAlchemy | 2.0+ | Async support |
| DB Driver | asyncpg | 0.29+ | Async PostgreSQL |

### Frontend
| Componente | Tecnología | Versión | Justificación |
|------------|------------|---------|---------------|
| Framework | Next.js | 14+ | SSR, SEO, performance |
| UI Library | React | 18+ | Component-based |
| Styling | Tailwind CSS | 3.4+ | Utility-first |
| Charts | Plotly.js | 2.27+ | Interactive, científico |
| Maps | Leaflet | 1.9+ | Open-source, ligero |
| State | Zustand | 4.5+ | Simple, no boilerplate |

### Bases de Datos
| Componente | Tecnología | Versión | Justificación |
|------------|------------|---------|---------------|
| OLTP | PostgreSQL | 16+ | Robusto, ACID |
| Time Series | TimescaleDB | 2.13+ | Hypertables para series |
| Analítico | DuckDB | 0.10+ | OLAP in-process |
| Cache | Redis | 7.2+ | In-memory, rápido |
| Vector DB | Weaviate | 1.23+ | Embeddings, RAG |

### ML/AI
| Componente | Tecnología | Versión | Justificación |
|------------|------------|---------|---------------|
| LLM | OpenAI GPT-4 | API latest | SOTA reasoning |
| Embeddings | text-embedding-ada-002 | API | Calidad/costo |
| Framework ML | scikit-learn | 1.4+ | Clásico, confiable |
| Boosting | XGBoost | 2.0+ | Gradient boosting |
| Forecasting | Prophet | 1.1+ | Feriados, estacionalidad |
| Deep Learning | PyTorch | 2.2+ | LSTMs para series |
| Tracking | MLflow | 2.10+ | Experimentos, modelos |
| Agent | LangChain | 0.1+ | RAG, tools, memory |

### Infraestructura
| Componente | Tecnología | Versión | Justificación |
|------------|------------|---------|---------------|
| Contenedores | Docker | 24+ | Aislamiento |
| Orquestación | Kubernetes | 1.29+ | Escalabilidad |
| Ingress | NGINX | 1.9+ | Load balancing |
| CI/CD | GitHub Actions | - | Integrado con repo |
| IaC | Terraform | 1.7+ | Infrastructure as Code |
| Service Mesh | Istio | 1.20+ (opcional) | Traffic management |

### Observabilidad
| Componente | Tecnología | Versión | Justificación |
|------------|------------|---------|---------------|
| Métricas | Prometheus | 2.49+ | Time series metrics |
| Dashboards | Grafana | 10.3+ | Visualización |
| Logs | ELK Stack | 8.12+ | Centralized logging |
| Tracing | Jaeger | 1.53+ (opcional) | Distributed tracing |
| Alertas | Alertmanager | 0.26+ | Notification routing |

### Seguridad
| Componente | Tecnología | Versión | Justificación |
|------------|------------|---------|---------------|
| Secrets | AWS KMS / Azure KV | - | Managed service |
| TLS Certs | Let's Encrypt | - | Free, automated |
| Auth | OAuth2 + Azure AD | - | SSO enterprise |
| Scanning | Trivy | 0.49+ | Container vulnerabilities |
| SAST | SonarQube | 10.4+ | Code quality |

---

## 🔧 PATRONES DE DISEÑO IMPLEMENTADOS

### 1. **Repository Pattern** (Acceso a datos)
```python
# backend/db/repositories/demanda_repository.py
class DemandaRepository:
    def __init__(self, session: AsyncSession):
        self.session = session
    
    async def get_by_date_range(self, start: date, end: date) -> List[Demanda]:
        query = select(Demanda).where(
            and_(Demanda.fecha >= start, Demanda.fecha <= end)
        )
        result = await self.session.execute(query)
        return result.scalars().all()
```

### 2. **Dependency Injection** (FastAPI)
```python
# backend/api/dependencies.py
async def get_db_session() -> AsyncGenerator[AsyncSession, None]:
    async with async_session_maker() as session:
        yield session

@app.get("/demanda")
async def get_demanda(
    db: AsyncSession = Depends(get_db_session),
    repo: DemandaRepository = Depends(get_demanda_repository)
):
    return await repo.get_all()
```

### 3. **Strategy Pattern** (Modelos ML)
```python
# ml/inference/strategy.py
class PredictionStrategy(ABC):
    @abstractmethod
    def predict(self, features: DataFrame) -> ndarray:
        pass

class ProphetStrategy(PredictionStrategy):
    def predict(self, features: DataFrame) -> ndarray:
        # Prophet implementation
        pass

class LSTMStrategy(PredictionStrategy):
    def predict(self, features: DataFrame) -> ndarray:
        # LSTM implementation
        pass
```

### 4. **Observer Pattern** (Alertas)
```python
# backend/alerts/observer.py
class AlertObserver(ABC):
    @abstractmethod
    def update(self, alert: Alert):
        pass

class EmailAlertObserver(AlertObserver):
    def update(self, alert: Alert):
        send_email(alert)

class SlackAlertObserver(AlertObserver):
    def update(self, alert: Alert):
        send_slack_message(alert)
```

### 5. **Circuit Breaker** (APIs externas)
```python
# backend/api/external/circuit_breaker.py
class CircuitBreaker:
    def __init__(self, failure_threshold: int = 5):
        self.failure_count = 0
        self.failure_threshold = failure_threshold
        self.state = "CLOSED"  # CLOSED, OPEN, HALF_OPEN
    
    async def call(self, func, *args, **kwargs):
        if self.state == "OPEN":
            raise CircuitBreakerOpenError()
        
        try:
            result = await func(*args, **kwargs)
            self.on_success()
            return result
        except Exception as e:
            self.on_failure()
            raise e
```

---

## 📈 ESCALABILIDAD Y PERFORMANCE

### Horizontal Scaling (Kubernetes)
```yaml
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: api-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: api-deployment
  minReplicas: 3
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
```

### Caching Strategy
```
┌─────────────────────────────────────────────────┐
│           CACHE LAYERS                           │
├─────────────────────────────────────────────────┤
│                                                  │
│  L1: Browser Cache (1 hora)                     │
│      - Assets estáticos (CSS, JS, imágenes)     │
│                                                  │
│  L2: CDN Cache (24 horas)                       │
│      - API responses públicas                   │
│                                                  │
│  L3: Redis Cache (TTL variable)                 │
│      - Demanda diaria: 1 hora                   │
│      - Predicciones: 6 horas                    │
│      - Documentos RAG: 24 horas                 │
│      - Sesiones usuario: 30 minutos             │
│                                                  │
│  L4: Query Cache (PostgreSQL)                   │
│      - Agregados precalculados                  │
│      - Materialized views                       │
│                                                  │
└─────────────────────────────────────────────────┘
```

### Database Optimization
```sql
-- Indices optimizados
CREATE INDEX idx_demanda_fecha ON demanda_nacional(fecha DESC);
CREATE INDEX idx_perdidas_or_fecha ON perdidas(or_id, fecha DESC);

-- Particionamiento (TimescaleDB)
SELECT create_hypertable('demanda_nacional', 'fecha', chunk_time_interval => INTERVAL '1 month');

-- Materialized views para agregados
CREATE MATERIALIZED VIEW demanda_mensual AS
SELECT 
    DATE_TRUNC('month', fecha) AS mes,
    AVG(demanda_gwh) AS demanda_promedio,
    MAX(demanda_gwh) AS demanda_maxima
FROM demanda_nacional
GROUP BY DATE_TRUNC('month', fecha);

-- Refresh automático cada noche
CREATE OR REPLACE FUNCTION refresh_materialized_views()
RETURNS void AS $$
BEGIN
    REFRESH MATERIALIZED VIEW CONCURRENTLY demanda_mensual;
END;
$$ LANGUAGE plpgsql;
```

---

## 🔄 DISASTER RECOVERY

### Backup Strategy
```
┌─────────────────────────────────────────────────┐
│           BACKUP TIERS                           │
├─────────────────────────────────────────────────┤
│                                                  │
│  Tier 1: Continuous (WAL Archiving)             │
│          - PostgreSQL WAL → S3                   │
│          - RPO: < 1 minuto                       │
│          - Retention: 7 días                     │
│                                                  │
│  Tier 2: Daily Incremental                      │
│          - 2:00 AM diario                        │
│          - Solo cambios del día                  │
│          - Retention: 3 meses                    │
│                                                  │
│  Tier 3: Weekly Full                            │
│          - Domingo 3:00 AM                       │
│          - Backup completo                       │
│          - Retention: 6 meses                    │
│                                                  │
│  Tier 4: Monthly Archive                        │
│          - Primer domingo del mes                │
│          - Backup completo + logs                │
│          - Retention: 7 años (legal)             │
│                                                  │
└─────────────────────────────────────────────────┘
```

### Recovery Procedures
```bash
# Recuperación Point-in-Time (PITR)
pg_basebackup -h backup-server -D /var/lib/postgresql/restore -U replicator -P
# Restaurar a timestamp específico
recovery_target_time = '2025-12-02 10:30:00'

# Recuperación completa desde backup
pg_restore -h localhost -U postgres -d siea -v backup_20251202.dump

# Verificación post-recuperación
psql -U postgres -d siea -c "SELECT COUNT(*) FROM demanda_nacional;"
psql -U postgres -d siea -c "SELECT MAX(fecha) FROM demanda_nacional;"
```

---

**FIN DE ARQUITECTURA SIEA**

*Documento técnico de referencia para el desarrollo e implementación del sistema.*
